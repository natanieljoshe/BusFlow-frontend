# Tugas Selesai ✅

1. ✅ http://localhost:8000/admin/routes/1 — Multi-bus per rute sudah bisa (hapus validasi di BusController)
2. ✅ http://localhost:8000/admin/routes/1 — Admin bisa edit biaya tiket via tombol "Edit Fare"
3. ✅ http://localhost:8000/admin/routes — Harga tiket tampil benar (parseFloat + toLocaleString id-ID)